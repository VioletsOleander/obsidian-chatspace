# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  run-hooks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup node
        uses: actions/setup-node@v7
        with:
          cache: "npm"
      - name: Install dependencies
        run: npm ci
      - name: Check format
        run: npx dprint check --allow-no-files
      - name: Check Type
        run: npx tsc
      - name: Lint
        run: npx eslint
      - name: Lint Svelte
        run: npx svelte-check
